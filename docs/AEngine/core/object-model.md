# Object Model

## Purpose

Defines the structure and lifecycle of runtime objects.

## Responsibilities

- object identity
- lifecycle management
- object relationships
- state containers

## Design rules

- objects must not implicitly depend on specific modules
- lifecycle must be explicit and observable
- object composition preferred over inheritance
