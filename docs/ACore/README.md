# ACore

ACore is the immutable platform foundation of AEngine.

## What ACore is

- host environment
- loads profiles, modules, plugins
- provides UI shell
- enforces plugin permissions
- provides shared infrastructure

## What ACore is not

- not a game engine
- not a project profile
- not a compiler
- does not run builds
